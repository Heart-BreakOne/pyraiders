

def place_the_unit(
                usable_markers,
                cap_id,
                raid,
                raid_id,
                name,
                user_id,
                token,
                user_agent,
                proxy,
                proxy_user,
                proxy_password,
                version,
                data_version):
    pass